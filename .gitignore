# Env & secrets
.env
*.env

# Virtual environments & deps cache
.venv/
__pycache__/
.pytest_cache/

# Runtime data
/data/
/logs/
*.sqlite

# Docker artifacts
*.log
*.pid

# Ops inventory (keep local-only; contains hostnames/IPs/paths)
ops/inventory.json

# Enclave build artifacts (never commit)
enclave/kmstool_enclave_cli
enclave/libnsm.so
enclave/*.eif
enclave-core/kmstool_enclave_cli
enclave-core/libnsm.so
enclave-core/*.eif
